---
id: "0001"
session: 01a0a348-d297-7fc7-8d43-a31063ef252d
entries: [1cf7a8b1, 4eb1745d]
at: 2026-09-15T15:56:24.809Z
messages: 23
trigger: threshold
---

## user

list all of the gizmo extensions in this repo

## assistant

### tool ls

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry"
}
```

#### ls → ok

```
.git/
.gitignore
extensions/
gizmo.registry.json
node_modules/
package.json
packages/
pnpm-lock.yaml
pnpm-workspace.yaml
README.md
scripts/
svelte.config.js
tests/
tsconfig.base.json
tsconfig.json
vitest.config.ts
vitest.setup.ts
```

## assistant

### tool ls

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions"
}
```

#### ls → ok

```
activity/
activity.web.js
ask-user/
ask-user.web.js
codex/
codex.web.js
git/
git.web.js
ollama/
ollama.web.js
search-and-scrape/
search-and-scrape.web.js
skill-authoring/
subagents/
subagents.web.js
svelte/
svelte.web.js
unity/
unity.web.js
workflows/
workflows.web.js
```

## assistant

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/gizmo.registry.json"
}
```

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/package.json"
}
```

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/README.md"
}
```

#### read → ok

```
{
  "extensionsDir": "extensions",
  "build": "pnpm install --frozen-lockfile && pnpm build",
  "extensions": [
    {
      "id": "activity",
      "name": "Activity",
      "description": "Inspect tool activity in the current workspace"
    },
    {
      "id": "ask-user",
      "name": "Ask the user",
      "description": "Let the agent ask multiple-choice questions with a native ch
…[truncated 1047 chars]…
ct, and steer background subagents with a live panel"
    },
    {
      "id": "workflows",
      "name": "Workflows",
      "description": "Multi-agent workflow orchestration with live run inspection"
    },
    {
      "id": "ollama",
      "name": "Ollama",
      "description": "Local LLM/SLM models via Ollama: install, update, pull, and manage models as a Pi provider"
    }
  ]
}
```

#### read → ok

```
{
  "name": "@gizmo/extension-registry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsx scripts/build-extensions.ts",
    "check": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.34.5",
  "
…[truncated 346 chars]…

    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/svelte": "^5.4.2",
    "@types/node": "^26.4.0",
    "bits-ui": "^2.19.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "prettier-plugin-svelte": "^4.1.1",
    "svelte": "^5.56.10",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "4.1.11"
  },
  "private": "true",
  "type": "module"
}
```

#### read → ok

```
# Gizmo Extension Registry

A user-curated Git registry for [Gizmo](https://github.com/) Pi extensions and their paired web UI.

## Layout

Each extension lives under `extensions/<id>/` with an `index.ts` Pi entrypoint, a `pi-extension.ts` implementation, and an optional `src/web/index.ts`. Building emits `extensions/<id>.web.js`. Gizmo directory-links the Pi extension and installs its optional browser bundle as one unit.

The Unity extension also owns its version-aware documentation tools (`unity_docs_*`), which discover and index documentation lazily for the active Unity workspace.

## Build

```sh
pnpm install
pnpm build
```

Add this repository's Git URL in Gizmo under **Settings → Extensions**, then link the extensions you want.
```

## assistant

This repo (`gizmo-registry`) contains **11 gizmo extensions**:

From `gizmo.registry.json` (`extensionsDir: extensions`):

1. **activity** — *Activity* — Inspect tool activity in the current workspace
2. **ask-user** — *Ask the user* — Let the agent ask multiple-choice questions with a native chat card
3. **git** — *Git* — Git status, changes, commit tools, and workspace integration
4. **codex** — *Codex* — Show OpenAI Codex usage and rate limits in the titlebar and a usage panel
5. **skill-authoring** — *Skill Authoring* — Skills for creating effective Pi skills and agent guidance
6. **svelte** — *Svelte* — Svelte project guidance and Gizmo workspace integration
7. **unity** — *Unity* — Unity tools, project services, console, and editor integration
8. **search-and-scrape** — *Search & Scrape* — SearXNG web search and Crawl4AI page scraping with native result cards
9. **subagents** — *Subagents* — Spawn, inspect, and steer background subagents with a live panel
10. **workflows** — *Workflows* — Multi-agent workflow orchestration with live run inspection
11. **ollama** — *Ollama* — Local LLM/SLM models via Ollama: install, update, pull, and manage models as a Pi provider

Each lives under `extensions/<id>/` with a Pi entrypoint (`index.ts` / `pi-extension.ts`) and optional web UI (`src/web/index.ts` → built to `extensions/<id>.web.js`).

## user

which of the extensions provide UI to the web app in some capacity

## assistant

### reasoning

I need to check which extensions have src/web/index.ts and built .web.js files.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions\" && for d in */; do id=\"${d%/}\"; echo \"== $id\"; ls -a \"$id\" 2>/dev/null | tr '\\n' ' '; echo; ls \"$id/src/web\" 2>/dev/null | tr '\\n' ' '; echo; done"
}
```

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry\" && ls -la extensions/*.web.js"
}
```

#### bash → ok

```
== activity
. .. index.ts pi-extension.ts src 
index.ts 
== ask-user
. .. index.ts pi-extension.ts src 
AskUserToolResult.svelte index.ts 
== codex
. .. index.ts pi-extension.ts src 
CodexPanel.svelte codex-runtime.svelte.ts codex.css index.ts types.ts 
== git
. .. index.ts pi-extension.ts src 
index.ts 
== ollama
. .. index.ts pi-extension.ts src 
OllamaPanel.svelte format.ts index.ts ollama.css
…[truncated 205 chars]…
lls 

== subagents
. .. index.ts pi-extension.ts src 
SubagentToolResult.svelte SubagentsPanel.svelte index.ts subagents-runtime.svelte.ts types.ts 
== svelte
. .. index.ts pi-extension.ts src 
index.ts 
== unity
. .. index.ts pi-extension.ts src 
index.ts 
== workflows
. .. index.ts pi-extension.ts src 
WorkflowToolResult.svelte WorkflowsPanel.svelte index.ts types.ts workflows-runtime.svelte.ts
```

#### bash → ok

```
-rw-r--r-- 1 mchan 197609  43731 Sep 14 09:40 extensions/activity.web.js
-rw-r--r-- 1 mchan 197609   5348 Sep 14 09:40 extensions/ask-user.web.js
-rw-r--r-- 1 mchan 197609  14224 Sep 14 09:40 extensions/codex.web.js
-rw-r--r-- 1 mchan 197609 227464 Sep 14 09:41 extensions/git.web.js
-rw-r--r-- 1 mchan 197609  19246 Sep 14 09:41 extensions/ollama.web.js
-rw-r--r-- 1 mchan 197609  11124 Sep 14 09:41 extensions/search-and-scrape.web.js
-rw-r--r-- 1 mchan 197609  24382 Sep 14 09:41 extensions/subagents.web.js
-rw-r--r-- 1 mchan 197609    120 Sep 14 09:41 extensions/svelte.web.js
-rw-r--r-- 1 mchan 197609 409707 Sep 14 09:41 extensions/unity.web.js
-rw-r--r-- 1 mchan 197609  33204 Sep 14 09:41 extensions/workflows.web.js
```

## assistant

### reasoning

skill-authoring has no src/web. svelte.web.js is 120 bytes - probably a stub. Let me check contents of svelte/src/web/index.ts, activity, git, unity web index.ts to see what they register (panels vs tool result renderers).

Let me read the small ones.

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions/svelte/src/web/index.ts"
}
```

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions/activity/src/web/index.ts"
}
```

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions/ask-user/src/web/index.ts"
}
```

#### read → ok

```
/** Svelte has no custom web UI; this keeps its domain id registered. */
export const gizmoWebExtension = {
  id: "svelte",
};
```

#### read → ok

```
export { gizmoWebExtension } from "../../../../packages/activity/src/web/index.ts";
```

#### read → ok

```
import AskUserToolResult from "./AskUserToolResult.svelte";

/**
 * Ask-user's web presentation, paired with the Pi extension of the same id.
 * The question card itself is Gizmo's generic bridge renderer (select/input
 * requests render there for every extension); this extension contributes the
 * tool's own presentation: its label, and a native rendering of the question
 * and the answer on the tool call.
 */
export const gizmoWebExtension = {
  id: "ask-user",
  labels: {
    ask_user: "Ask the user",
  },
  parametersFor: (name: string, parameters: [string, string][]) =>
    name === "ask_user"
      ? parameters.filter(([param]) => param === "question")
      : parameters,
  resultFor: (name: string) =>
    name === "ask_user" ? AskUserToolResult : undefined,
};
```

## assistant

### reasoning

Now check git, unity, svelte (already: none), codex, ollama, subagents, workflows, search-and-scrape. Let's peek at git and unity web index and codex/ollama/subagents/workflows briefly.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions\" && for f in git/src/web/index.ts unity/src/web/index.ts codex/src/web/index.ts search-and-scrape/src/web/index.ts; do echo \"===== $f\"; head -40 \"$f\"; done"
}
```

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/extensions\" && for f in ollama/src/web/index.ts subagents/src/web/index.ts workflows/src/web/index.ts; do echo \"===== $f\"; head -30 \"$f\"; done"
}
```

#### bash → ok

```
===== git/src/web/index.ts
export { gizmoWebExtension } from "../../../../packages/git/src/web/index";
===== unity/src/web/index.ts
export { unityWebExtension as gizmoWebExtension } from "../../../../packages/unity/src/web/index.ts";
===== codex/src/web/index.ts
import { usageTone } from "../usage.ts";
import { activeCodexRuntime, CodexRuntime } from "./codex-runtime.svelte.ts";
import type { Ext
…[truncated 1453 chars]…
 "source"].includes(param),
      );
    }
    if (name === "scrape") {
      return parameters.filter(([param]) =>
        ["url", "onlyMainContent", "waitFor", "timeout"].includes(param),
      );
    }
    return parameters;
  },
  resultFor: (name: string) => {
    if (name === "search") return SearchToolResult;
    if (name === "scrape") return ScrapeToolResult;
    return undefined;
  },
};
```

#### bash → ok

```
===== ollama/src/web/index.ts
import OllamaPanel from "./OllamaPanel.svelte";
import type { OllamaInspectorTab } from "./types";

/**
 * Ollama's web presentation, paired with the Pi extension of the same id: a
 * workspace-inspector tab for managing the local Ollama host and its models.
 * Machine-global concerns, surfaced through the workspace's extension invoke
 * channel; no per-projec
…[truncated 1828 chars]…
on,
  activate: workflowsExtension.activate,
  labels: {
    workflow: "Workflow",
  },
  parametersFor: (name: string, parameters: [string, string][]) =>
    name === "workflow"
      ? parameters.filter(([param]) => param === "background")
      : parameters,
  resultFor: (name: string) =>
    name === "workflow" ? WorkflowToolResult : undefined,
};

export type { RunSummary, WorkflowRunView };
```

## assistant

### reasoning

Now check packages/activity and packages/git and packages/unity web modules — do they actually provide UI? activity.web.js is 43KB, git.web.js 227KB, unity.web.js 409KB. Let's check what they export.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/packages\" && ls && echo \"--- activity web\" && head -60 activity/src/web/index.ts 2>/dev/null && echo \"--- git web\" && head -50 git/src/web/index.ts 2>/dev/null && echo \"--- unity web\" && head -60 unity/src/web/index.ts 2>/dev/null"
}
```

#### bash → ok

```
activity
design
extensions
git
orchestration
protocol
svelte
ui
unity
unity-tools
--- activity web
import type { Component } from "svelte";
import type { ToolCallView } from "@gizmo/protocol";
import ActivityPanel from "./ActivityPanel.svelte";

export interface ActivityInspectorTab {
  id: string;
  label: string;
  component: Component<any>;
  props: Record<string, unknown>;
}

/** Activity's s
…[truncated 1578 chars]…
  extensionId: string,
    operation: string,
    input?: unknown,
  ): Promise<unknown>;
}

async function commitAll(store: GitHostStore): Promise<void> {
  try {
    const message = await store.generateCommitMessage();
    const result = await store.commitAll(message);
    toasts.show(`Committed ${result.commit.slice(0, 7)}`);
--- unity web
export { unityWebExtension } from "./gizmo-extension";
```

## assistant

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/packages/unity/src/web\" && ls && echo \"=====\" && head -80 gizmo-extension.ts"
}
```

#### bash → ok

```
UnityDomainDialog.svelte
UnityDomainSettings.svelte
console
design
domain-plugin.test.ts
domain-plugin.ts
gizmo-extension.ts
host.ts
index.ts
tool-presentation.ts
types.ts
unity
unity-wire.ts
=====
import { unityDomainPlugin } from "./domain-plugin";
import { unityToolPresentation } from "./tool-presentation";
import { unityExtension } from "./console/console-extension.svelte";

/** Unity's singl
…[truncated 327 chars]…
tatus,
  apiVersion: unityExtension.apiVersion,
  activate: unityExtension.activate,
  labels: unityToolPresentation.labels,
  iconFor: unityToolPresentation.iconFor,
  consoleEntriesKey: unityToolPresentation.consoleEntriesKey,
  parametersFor: unityToolPresentation.parametersFor,
  resultFor: unityToolPresentation.resultFor,
  diagnosticsComponent: unityToolPresentation.diagnosticsComponent,
};
```

## assistant

**9 of the 11** ship real web UI. One more registers a web bundle but draws nothing, and one has no web bundle at all.

**Rich UI (inspector tabs / panels / status bar / commands):**

| Extension | Web contributions |
|---|---|
| **activity** | Inspector tab (`ActivityPanel`, fed by `toolActivity`) |
| **codex** | Activated runtime + titlebar/status-bar usage indicator + live Codex usage inspector tab |
| **git** | Changes inspector tab, command palette commands (e.g. commit-all), status-bar items |
| **ollama** | Workspace-inspector tab (`OllamaPanel`) for managing the local Ollama host/models |
| **subagents** | Native cards for the 5 `subagent_*` tools + live Subagents inspector tab |
| **workflows** | Native card for the `workflow` tool (phases, agents, progress) + Workflows inspector tab |
| **unity** | The most: dialog, settings, inspector tabs, commands, project status, tool labels/icons/result renderers, plus a diagnostics component and console entry rendering |

**Tool-presentation only (result cards / labels, no panels):**

| Extension | Web contributions |
|---|---|
| **ask-user** | `AskUserToolResult.svelte` card + `ask_user` label/param filtering |
| **search-and-scrape** | `SearchToolResult.svelte` + `ScrapeToolResult.svelte` cards, labels, param filtering |

**Not really UI:**

- **svelte** — `src/web/index.ts` is a 120-byte stub: `export const gizmoWebExtension = { id: "svelte" }`, commented *"Svelte has no custom web UI; this keeps its domain id registered."*
- **skill-authoring** — no `src/web/` at all (just `index.ts`, `pi-extension.ts`, `skills/`), so no `<id>.web.js` is built.

Note the shared-heavy ones delegate to packages: `activity` → `packages/activity`, `git` → `packages/git`, `unity` → `packages/unity` (which composes `unityDomainPlugin` + `unityToolPresentation` + `console/console-extension.svelte`).

### reasoning

So 10 of 11 ship web UI; only skill-authoring has no src/web (skills only). But "provide UI to the web app in some capacity": svelte is a no-op stub (120 bytes) that just registers the id, no UI. So:

Real UI: activity, ask-user, codex, git, ollama, search-and-scrape, subagents, unity, workflows (9). 
Svelte: stub, no UI (registers domain id only).
Skill-authoring: no web bundle at all.

Let me build the answer with what each contributes. Also confirm skill-authoring has no web dir (confirmed: only index.ts, pi-extension.ts, skills).

Let me double check unity's dialog/settings/host - yes. And git web.js is 227KB with commands, statusbar, inspector tabs.

Group by contribution type.

## user

will editing an extension cause issues for gizmo agents using the extensions in other threads?

## assistant

### reasoning

The user asks: will editing an extension cause issues for gizmo agents using the extensions in other threads? This is a question about how Gizmo loads extensions — directory-linking, build output, hot reload. I should inspect the repo for facts: build script, how extensions are linked (README says "Gizmo directory-links the Pi extension and installs its optional browser bundle as one unit"), whether builds overwrite .web.js, whether editing index.ts/pi-extension.ts affects running sessions.

Let me look at scripts/build-extensions.ts, and maybe the protocol package for extension loading. Also journal_search for prior notes.

Let me investigate.

### tool read

```json
{
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry/scripts/build-extensions.ts"
}
```

### tool journal_search

```json
{
  "query": "gizmo extension registry build directory-link reload running threads"
}
```

#### read → ok

```
import { access, readdir } from "node:fs/promises";
import { join } from "node:path";
import { buildWebExtension } from "./build-web-extension.ts";

const extensionsDir = join(import.meta.dirname, "..", "extensions");
const entries = await readdir(extensionsDir, { withFileTypes: true });

for (const entry of entries) {
  if (!entry.isDirectory()) continue;
  const extensionDir = join(extensionsDir, entry.name);
  const webEntry = join(extensionDir, "src", "web", "index.ts");
  try {
    await access(webEntry);
  } catch {
    continue;
  }
  console.log(
    await buildWebExtension(
      extensionDir,
      join(extensionsDir, `${entry.name}.web.js`),
    ),
  );
}
```

#### journal_search → ok

```
No matches for gizmo extension registry build directory-link reload running threads across 0 journal segments.
```

## assistant

### reasoning

Journal has 0 segments apparently. Let me check the build-web-extension script and how Gizmo loads extensions (maybe in node_modules @earendil-works/pi-coding-agent — extension loading). Let me search node_modules for extension loading / reload behavior.

Also check git status to see if there are in-flight edits.

Key question: does editing source affect running agents? Facts to gather:
1. Does gizmo link extension dir directly (so index.ts is loaded from source at session start)? README says "Gizmo directory-links the Pi extension" — suggests the Pi extension is loaded from the directory (source), so editing files could affect newly started threads, and possibly hot-reload in running ones.
2. Web bundle is a build artifact (extensions/<id>.web.js) — editing src/web requires rebuild; the built file is what's served.

Let me search node_modules for extension loader.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry\" && cat scripts/build-web-extension.ts 2>/dev/null | head -60 && echo \"=== git status\" && git status --porcelain && git log --oneline -5"
}
```

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/node_modules/@earendil-works\" && ls && grep -rl \"extensionsDir\\|gizmoWebExtension\\|web.js\" --include=*.js --include=*.ts --include=*.json . 2>/dev/null | head -20"
}
```

#### bash → ok

```
/**
 * Builds a Gizmo web extension into a single standalone ES module that the app
 * can load at runtime.
 *
 * The app's own bundle is produced by Vite, which resolves import specifiers by
 * static analysis at build time — so it can never see a plugin installed later.
 * The way around that is to build the plugin separately and load it through a
 * genuinely runtime `import()`, which the JS e
…[truncated 2219 chars]…
rds
=== git status
6ad00e6 Add Ollama management UI and registry catalog entry
2d1568e Wire ollama management operations and Pi provider registration
de14078 Add ollama extension foundation: REST client, job registry, host lifecycle
52746f0 Fix subagents panel, scope subagents and workflows to the open thread, and harden other extensions
00e1b2d Extract extension CSS and refresh extension tooling
```

#### bash → ok

```
pi-ai
pi-coding-agent
pi-tui
```

## assistant

### reasoning

No local loader info in node_modules. The Gizmo app loads extensions — I can't inspect the app itself (it's a separate app). Let me check if Gizmo app source is available somewhere (e.g., C:/Users/mchan/gizmo or similar). Let me look for it.

Also check docs in repo about loading behavior — maybe packages/protocol or packages/extensions has docs, or README mentions "directory-links".

Let's search for gizmo app source.

### tool bash

```json
{
  "command": "ls \"C:/Users/mchan\" 2>/dev/null | head -40; ls \"C:/Users/mchan/.gizmo\" 2>/dev/null | head -40"
}
```

### tool grep

```json
{
  "pattern": "directory-link|reload|restart|hot|live|install|link\",",
  "path": "C:/Users/mchan/.gizmo/registries/gizmo-registry",
  "glob": "*.md",
  "ignoreCase": true
}
```

#### bash → ok

```
AnimDemo
AppData
Application Data
C:Usersmchanjobdocker
Contacts
Cookies
CursorUnityPackage
Documents
Downloads
Favorites
Library
LilBigBoy
Links
Little Big Boy L.L.C
Local Settings
Matthew.nutt.e Dropbox
Music
My Documents
NTUSER.DAT
NTUSER.DAT{2ad838bc-efea-11ee-a54d-000d3a94eaa1}.TM.blf
NTUSER.DAT{2ad838bc-efea-11ee-a54d-000d3a94eaa1}.TMContainer00000000000000000001.regtrans-ms
NTUSER.DAT{2ad8
…[truncated 7 chars]…
ea-11ee-a54d-000d3a94eaa1}.TMContainer00000000000000000002.regtrans-ms
NetHood
OBS
OLD PHONE
Obsidian
OneDrive
PaceKeyChain
PrintHood
Recent
RiderProjects
Saved Games
Searches
SendTo
Splice
Start Menu
Templates
Unity user templates
UnityProjects
Videos
AGENTS.md
auth.json
config.json
models-store.json
models.json
projects.json
registries
resources.json
sessions
settings.json
skills
workspace.json
```

#### grep → ok

```
extensions/skill-authoring/skills/writing-for-agents/SKILL.md:35: 3. **Disclosed reference** is pushed out into a separate file, reached by a context pointer, loaded only when the pointer fires. Spans a sibling file in the same folder through fully external reference that lives anywhere and any document can point at.
extensions/skill-authoring/skills/writing-for-agents/SKILL.md:43: **Sprawl** is 
…[truncated 2349 chars]…
it.
README.md:7: Each extension lives under `extensions/<id>/` with an `index.ts` Pi entrypoint, a `pi-extension.ts` implementation, and an optional `src/web/index.ts`. Building emits `extensions/<id>.web.js`. Gizmo directory-links the Pi extension and installs its optional browser bundle as one unit.
README.md:14: pnpm install

[Some lines truncated to 500 chars. Use read tool to see full lines]
```

## assistant

### reasoning

Gizmo app source isn't visibly available. Let me check settings.json / config.json in .gizmo for extension registry config, and maybe check whether there's an installed app path (e.g. C:\Users\mchan\AppData\Local\...Gizmo). Also check .gizmo/registries — maybe multiple registries and an installed cache.

Actually, better: check if there's a copy of gizmo app source on disk somewhere (RiderProjects?). Let's search for "gizmo" directories.

Actually, the key question is about behavior of the running app: does Gizmo load extension JS at session start or hot-reload? That's app behavior I can part
…[truncated 271 chars]…
inked, an in-progress edit could hit a thread that starts during the edit (partially written file) → transient error.
- Web bundle: built artifact `<id>.web.js` — editing src/web requires rebuild; the app loads the built file. Overwriting the .web.js while the app has loaded it is fine (already fetched), but new page loads get new bundle. Also building rewrites the same filename, so a mid-build fetch could get a truncated file.

Let me check if the app is running to see loaded extension modules? Maybe check for gizmo app processes / installation to verify how it loads. Let's look for the app.

### tool bash

```json
{
  "command": "ls \"C:/Users/mchan/.gizmo/registries\"; echo \"---\"; cat \"C:/Users/mchan/.gizmo/settings.json\" 2>/dev/null | head -60"
}
```

### tool bash

```json
{
  "command": "where.exe gizmo 2>/dev/null; ls \"C:/Users/mchan/AppData/Local\" 2>/dev/null | grep -i -E \"gizmo|pi\" ; ls \"C:/Users/mchan/AppData/Roaming\" 2>/dev/null | grep -i -E \"gizmo|earendil\""
}
```

#### bash → ok

```
gizmo-registry
gizmo-skills-registry
installed.json
---
{
  "lastChangelogVersion": "0.84.3",
  "defaultProvider": "openai-codex",
  "defaultModel": "gpt-5.6-terra",
  "defaultThinkingLevel": "low",
  "theme": "dark",
  "hideThinkingBlock": false
}
```

#### bash → error

```
Epic Games
EpicGamesLauncher
PinballFX
PioneerGame
github-copilot
main.kts.compiled.cache
pinterest-dl
pip


Command exited with code 1
```

## assistant

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries\" && head -c 3000 installed.json"
}
```

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries\" && python -c \"\nimport json\nd=json.load(open('installed.json'))\nprint(type(d))\nprint(json.dumps(d, indent=2)[:4000])\n\" 2>/dev/null || cat installed.json | head -100"
}
```

#### bash → ok

```
{
  "registries": [
    {
      "name": "gizmo-registry",
      "url": "https://github.com/mcread29/gizmo-registry.git",
      "commit": "fa038ec",
      "addedAt": 1787933616195,
      "linked": [
        "ask-user",
        "activity",
        "git",
        "svelte",
        "unity",
        "skill-authoring",
        "search-and-scrape",
        "subagents",
        "workflows",
        "codex",
        "ollama"
      ],
      "extensions": []
    },
    {
      "name": "gizmo-skills-registry",
      "url": "https://github.com/mcread29/gizmo-skills-registry.git",
      "commit": "1087ff7",
      "addedAt": 1788219274886,
      "linked": [
        "matt-pocock-skills",
        "pstack",
        "agent-journal"
      ],
      "extensions": []
    }
  ]
}
```

#### bash → ok

```
<class 'dict'>
{
  "registries": [
    {
      "name": "gizmo-registry",
      "url": "https://github.com/mcread29/gizmo-registry.git",
      "commit": "fa038ec",
      "addedAt": 1787933616195,
      "linked": [
        "ask-user",
        "activity",
        "git",
        "svelte",
        "unity",
        "skill-authoring",
        "search-and-scrape",
        "subagents",
  
…[truncated 19 chars]…

        "codex",
        "ollama"
      ],
      "extensions": []
    },
    {
      "name": "gizmo-skills-registry",
      "url": "https://github.com/mcread29/gizmo-skills-registry.git",
      "commit": "1087ff7",
      "addedAt": 1788219274886,
      "linked": [
        "matt-pocock-skills",
        "pstack",
        "agent-journal"
      ],
      "extensions": []
    }
  ]
}
```

## assistant

### reasoning

Key: the registry is git-tracked by the app at a commit (fa038ec) — Gizmo updates registries by pulling/checkout. Linked extensions are loaded from the local registry dir.

Note: my earlier `git log` showed HEAD at 6ad00e6 with ollama commits. The installed.json commit fa038ec differs (probably app's last pulled commit).

Now what happens when I edit on disk? The app reads from the working tree of the registry (directory-link). So:
1. Edits are visible immediately to any newly started agent session (pi process imports extension source at startup) and to the app if it re-reads.
2. Risk windows
…[truncated 68 chars]…
uild .web.js.

Also important: the app may `git pull` / reset the registry when it updates — uncommitted work could be clobbered or block the update. Let me check whether there's evidence: is the working tree dirty? git status --porcelain returned nothing earlier (clean). Also note the registry dir may be checked out detached at a commit.

Let me check git branch state and whether HEAD matches installed commit; also check .gizmo/registries/gizmo-registry for remotes.

Also there's a question of the app "reload extensions" behavior. Can I find the Gizmo app install to inspect? Check processes.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry\" && git status -sb && git log --oneline -1 && git rev-parse HEAD && git remote -v"
}
```

### tool powershell

```json
{
  "command": "Get-Process | Where-Object { $_.ProcessName -match 'gizmo|tauri|pi' } | Select-Object Id, ProcessName, Path -First 20 | Format-List"
}
```

#### bash → ok

```
## main...origin/main [ahead 3]
6ad00e6 Add Ollama management UI and registry catalog entry
6ad00e69e31521326589210475b9a2785fc9ea9f
origin	git@github.com:mcread29/gizmo-registry.git (fetch)
origin	git@github.com:mcread29/gizmo-registry.git (push)
```

#### powershell → ok

```
[32;1mId          : [0m15384
[32;1mProcessName : [0mPowerToys.ColorPickerUI
[32;1mPath        : [0mC:\Users\mchan\AppData\Local\PowerToys\PowerToys.ColorPickerUI.exe
```

## assistant

### reasoning

Gizmo app isn't currently running (no gizmo process). But the user asks about agents in other threads — presumably other sessions running elsewhere (maybe Gizmo is running with a different process name, or not running now). Either way answer generally.

Let me check the pi-coding-agent package for extension loading semantics: does it load extension modules at process start (import) with caching? Check node_modules/@earendil-works/pi-coding-agent for "extension" loading code.

Also check whether sessions directory shows running sessions.

Let me grep pi-coding-agent for extension loading.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/node_modules/@earendil-works/pi-coding-agent\" && ls && grep -rl \"loadExtension\\|import(\" --include=*.js dist 2>/dev/null | head -10"
}
```

### tool bash

```json
{
  "command": "ls -la \"C:/Users/mchan/.gizmo/sessions\" 2>/dev/null | head -20; cat \"C:/Users/mchan/.gizmo/workspace.json\" 2>/dev/null | head -20"
}
```

#### bash → ok

```
CHANGELOG.md
README.md
dist
docs
examples
node_modules
npm-shrinkwrap.json
package.json
dist/bun/cli.js
dist/bundle/chunks/anthropic-messages-JWX2WP65.js
dist/bundle/chunks/anthropic.js
dist/bundle/chunks/bedrock-converse-stream.js
dist/bundle/chunks/chunk-2KVJKXS2.js
dist/bundle/chunks/chunk-MNAIPA3J.js
dist/bundle/chunks/chunk-NUHFSC37.js
dist/bundle/chunks/chunk-OMWWHBTG.js
dist/bundle/chunks/image-resize-worker.js
dist/bundle/chunks/openai-codex.js
```

#### bash → ok

```
total 5672
drwxr-xr-x 1 mchan 197609       0 Sep 15 08:51 .
drwxr-xr-x 1 mchan 197609       0 Sep 15 08:55 ..
-rw-r--r-- 1 mchan 197609    1996 Aug 26 08:40 2026-08-26T15-19-13-437Z_01a03ea7-671d-7046-8bcd-f4e8e0181978.jsonl
-rw-r--r-- 1 mchan 197609    3986 Aug 26 12:27 2026-08-26T17-46-23-057Z_01a03f2e-21d1-7473-98ca-068df688c2f5.jsonl
-rw-r--r-- 1 mchan 197609    3789 Aug 26 12:28 2026-08-26T1
…[truncated 753 chars]…
r-- 1 mchan 197609     843 Sep 15 08:51 2026-09-15T15-51-26-478Z_01a0a5c4-160e-7fd9-9e30-b57b15bcfa08.jsonl
-rw-r--r-- 1 mchan 197609  228026 Sep 15 08:55 2026-09-15T15-51-49-554Z_01a0a5c4-7032-75f9-b28b-5aa7159318cf.jsonl
drwxr-xr-x 1 mchan 197609       0 Sep 14 21:17 archived
drwxr-xr-x 1 mchan 197609       0 Sep 14 21:08 attachments
{
  "lastSessionId": "01a0a348-d297-7fc7-8d43-a31063ef252d"
}
```

## assistant

### reasoning

Sessions are jsonl; each session is a thread. The pi process for a session imports extension code at start. Let me look for how extensions are loaded in pi-coding-agent docs — check docs folder for extension docs.

I want evidence for pi extension loading: "extensions are loaded when the session starts" and "changes require restart/new session". Search docs.

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/node_modules/@earendil-works/pi-coding-agent\" && ls docs && grep -ril \"extension\" docs | head"
}
```

#### bash → ok

```
compaction.md
containerization.md
custom-provider.md
development.md
docs.json
environment-variables.md
extensions.md
images
index.md
json.md
keybindings.md
llama-cpp.md
models.md
packages.md
prompt-templates.md
providers.md
quickstart.md
rpc.md
sdk.md
security.md
session-format.md
sessions.md
settings.md
shell-aliases.md
skills.md
terminal-setup.md
termux.md
themes.md
tmux.md
tui.md
usage.md
windows.md
docs/compaction.md
docs/containerization.md
docs/custom-provider.md
docs/docs.json
docs/extensions.md
docs/index.md
docs/keybindings.md
docs/models.md
docs/packages.md
docs/providers.md
```

## assistant

### tool bash

```json
{
  "command": "cd \"C:/Users/mchan/.gizmo/registries/gizmo-registry/node_modules/@earendil-works/pi-coding-agent\" && grep -n -i -E \"reload|restart|loaded|load|hot|cach|lifecycle|activate|start\" docs/extensions.md | head -60"
}
```

#### bash → ok

```
5:Extensions are TypeScript modules that extend pi's behavior. They can subscribe to lifecycle events, register custom tools callable by the LLM, add commands, and more.
7:> **Placement for /reload:** Put extensions in `~/.pi/agent/extensions/` (global) or `.pi/extensions/` (project-local) for auto-discovery. Use `pi -e ./path.ts` only for quick tests. Extensions in auto-discovered locations can 
…[truncated 5268 chars]…
7:#### agent_start / agent_end / agent_settled
569:`agent_start` fires when a low-level agent run begins. `agent_end` fires when that run ends, but Pi may still auto-retry, auto-compact and retry, or continue with queued follow-up messages. Use `agent_settled` for status integrations that need to know Pi will not continue running automatically.
572:pi.on("agent_start", async (_event, ctx) => {});
```

## assistant

### reasoning

Key facts:
- Extensions loaded via jiti (TypeScript, no compilation) — so the Pi side imports source directly from the linked directory at process startup.
- `/reload` hot-reloads extensions in auto-discovered locations (`~/.pi/agent/extensions/` global, `.pi/extensions/` project-local). Note: registry-linked extensions are directory-linked — need to check whether Gizmo symlinks them into `~/.pi/agent/extensions`. Earlier we saw `C:\Users\mchan\.pi\agent\extensions` exists (from AGENTS.md path and skills list: C:\Users\mchan\.pi\agent\extensions\agent-journal etc.). Let me check whether these are symlinks/junctions into the registry directories.

That's important: if ~/.pi/agent/extensions/<id> is a junction to the registry dir, then edits are live immediately and /reload picks them up; running sessions only reload on /reload or new session.

Let me check.

### tool bash

```json
{
  "command": "ls -la \"C:/Users/mchan/.pi/agent/extensions\" 2>/dev/null; echo \"--- skills\"; ls -la \"C:/Users/mchan/.pi/agent/skills\" 2>/dev/null | head"
}
```

### tool powershell

```json
{
  "command": "Get-ChildItem \"C:\\Users\\mchan\\.pi\\agent\\extensions\" | Select-Object Name, LinkType, Target, Attributes | Format-Table -AutoSize | Out-String -Width 200"
}
```

#### bash → ok

```
total 37
drwxr-xr-x 1 mchan 197609    0 Sep 14 10:49 .
drwxr-xr-x 1 mchan 197609    0 Sep 15 08:53 ..
lrwxrwxrwx 1 mchan 197609   67 Aug 31 16:37 activity -> /c/Users/mchan/.gizmo/registries/gizmo-registry/extensions/activity
lrwxrwxrwx 1 mchan 197609   79 Sep  1 17:29 agent-journal -> /c/Users/mchan/.gizmo/registries/gizmo-skills-registry/extensions/agent-journal
lrwxrwxrwx 1 mchan 197609   67 A
…[truncated 1780 chars]…
tensions/workflows
--- skills
total 20
drwxr-xr-x 1 mchan 197609  0 Sep  2 11:42 .
drwxr-xr-x 1 mchan 197609  0 Sep 15 08:53 ..
drwxr-xr-x 1 mchan 197609  0 Jul 18 14:07 archify
lrwxrwxrwx 1 mchan 197609 38 Sep  2 11:42 here-now -> /c/Users/mchan/.agents/skills/here-now
drwxr-xr-x 1 mchan 197609  0 Jul 18 13:45 svelte-code-writer
drwxr-xr-x 1 mchan 197609  0 Jul 18 13:51 svelte-core-bestpractices
```

#### powershell → ok

```
Name                            LinkType Target                                                                                            Attributes
----                            -------- ------                                                                                            ----------
activity                        Junction C:\Users\mchan\.gizmo\registries\gizmo-registry\extensio
…[truncated 2371 chars]…
han\.gizmo\registries\gizmo-registry\extensions\workflows                 Directory, ReparsePoint
herdr-agent-state.ts.bak                                                                                                                      Archive
pi-codex-image-gen-install.json                                                                                                               Archive
```
